# FunRounds

ModSharp CS2 fun/special round engine. Core + `.Shared` API; round packs are 3rd-party modules (AWP/Deagle pack bundled). Logic reworked from laikiux-lt/awp (CSS) — rewritten to ModSharp standards, not a line port.
